/**
 * Empresa Admin — Generar Reportes (new feature).
 * Wraps the EmpresaReportes client component.
 */
import EmpresaReportes from "@/components/dashboard/empresa/EmpresaReportes";

export default function EmpresaReportesPage() {
  return <EmpresaReportes />;
}
